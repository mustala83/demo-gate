name: required-ci
on: [push, pull_request]
jobs:
  security-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Refuse unsafe changes
        run: |
          echo "security test FAILED - this change must not be merged"
          exit 1
